# APRP mirror stack environment example.
# Keep only secrets and machine-local auth here. Non-secret config comes
# from `ops/opsconfig.yaml`.
DB_ROOT_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me
